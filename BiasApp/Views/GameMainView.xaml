<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
             Style="{StaticResource BaseContent}"
             x:Class="BiasApp.Views.GameMainView">

    <StackLayout>
        
        <Label Text="Multiplayer Spil"
               Style="{StaticResource PageTitle}"/>
        
        <BoxView Style="{StaticResource YellowSeparator}"/>
        
        <Button Text="Host Multiplayer Spil"
                Clicked="HostGameButton_Clicked"
                Style="{StaticResource BlueButton}"/>

        <Button Text="Join Multiplayer Spil"
                Clicked="JoinGameButton_Clicked"
                Style="{StaticResource RedButton}"/>

        <!-- To be replaced by a presentation video wich will contain the presentation of the multiplayer game -->
        <Frame Style="{StaticResource LargeVideoFrame}">
            <xct:MediaElement Source="ms-appx:///funny_animal_noises.mp4"
                              Style="{StaticResource SoundVideo}"/>
        </Frame>

        <Button Text="Tilbage"
                Clicked="BackButton_Clicked"
                Style="{StaticResource BackButton}"/>
    </StackLayout>
</ContentPage>